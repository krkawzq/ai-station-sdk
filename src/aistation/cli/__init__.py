"""aistation CLI — read-only dashboard and query interface.

Run ``aistation --help`` for the full command list. The CLI intentionally
covers only login + information retrieval. Write operations (create / delete
/ stop / commit) are available only through the Python SDK.
"""
